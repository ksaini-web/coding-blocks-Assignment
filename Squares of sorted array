#include<bits/stdc++.h>
using namespace std; 

int main(){

int N;
cin>>N;

vector<int>nums(N);

for(int i=0;i<N;i++){

    cin>>nums[i];

    nums[i]=abs(nums[i]);
}
sort(numsnums.begin(), nums.end());



for(int i=0;i<N;i++){

    cout<<(nums[i]*nums[i])<<" ";}

return 0;
}
